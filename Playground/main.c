
#include <stdlib.h>
#include <stdio.h>


Item dotProduct(Index len, const Vector v1, const Vector v2) {
    int new[len];
    for (int m = 0; m < k; m++) {
        for (int k = 0; k < len; k++) {
            new = v1[m] * v2[k];
        }
    }
    return new;
}

int main(void) {


    return EXIT_SUCCESS;
}